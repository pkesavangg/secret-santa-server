// This file will contain routes for the Secret Santa API
// To be implemented later with Firebase Authentication

const express = require('express');
const router = express.Router();

router.get('/placeholder', (req, res) => {
  res.json({ message: 'Group routes will be implemented later' });
});

module.exports = router;
